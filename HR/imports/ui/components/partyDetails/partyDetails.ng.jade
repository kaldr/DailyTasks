| The party you selected is:
form
  | Party name: 
  input(type='text', ng-model='partyDetails.party.name', ng-disabled='!partyDetails.isOwner')
  |   Description: 
  input(type='text', ng-model='partyDetails.party.description', ng-disabled='!partyDetails.isOwner')
  |   Public Party? 
  input(type='checkbox', ng-model='partyDetails.party.public', ng-disabled='!partyDetails.isOwner')
  button(ng-click='partyDetails.save()') Save

button(ui-sref='parties') Back
party-uninvited(party="partyDetails.party" ng-show="partyDetails.canInvite()")
party-map(location='partyDetails.party.location')
ul
  | Users:
  li(ng-repeat="user in partyDetails.users")
    | {{user.emails[0].address}}
